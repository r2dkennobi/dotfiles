---
- name: Full setup
  hosts: localhost
  tasks:
    - include_role:
        name: base
    - include_role:
        name: bat-ansible
    - include_role:
        name: prettyping-ansible
    - include_role:
        name: diff-so-fancy-ansible
    - include_role:
        name: rust-ansible
